# Copy to .env and fill in the keys for the models you use.
# The harness only reads keys from the environment — never from the config file.

DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Ollama runs locally and needs no key.
# Optional integrations:
ZAPIER_API_KEY=
